openapi: 3.1.0
info:
  title: Schema Type Change API
  version: 1.1.0
paths:
  /users:
    get:
      operationId: listUsers
      responses:
        "200":
          description: User payload
          content:
            application/json:
              schema:
                type: object
                properties:
                  age:
                    type: integer
