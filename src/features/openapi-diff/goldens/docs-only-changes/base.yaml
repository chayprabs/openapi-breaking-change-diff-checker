openapi: 3.1.0
info:
  title: Docs Only API
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      summary: List users
      description: Returns all users.
      responses:
        "200":
          description: User payload
