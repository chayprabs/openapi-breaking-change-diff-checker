openapi: 3.1.0
info:
  title: Docs Only API
  version: 1.1.0
paths:
  /users:
    get:
      operationId: listUsers
      summary: List all active users
      description: Returns all users with rollout guidance.
      responses:
        "200":
          description: User payload for documented clients
